# 라마(Llama) 실험 노트

**담당** 최성호 · Llama 모델 튜닝 및 전처리
**브랜치** `feature/llama`
**최종 갱신** 2026-07-27

---

## 실행 환경

| 항목 | 값 |
|---|---|
| GPU | RTX 4070 Laptop (VRAM 8GB) |
| PyTorch | 2.5.1+cu121 (CUDA 사용 가능) |
| 모델 | `meta-llama/Llama-3.2-3B-Instruct` |
| 양자화 | 4bit (QLoRA 방식, `--load_in_4bit`) |
| 벡터DB | FAISS |
| 임베딩 | `jhgan/ko-sbert-nli` (**최대 입력 128토큰**) |
| 청크 | 500자 / overlap 50자 → 총 1,953개 |
| 평가셋 | 14문항 (single 9 / multi 3 / unanswerable 2) |

### 8GB VRAM에서 3B 모델을 선택한 이유

Llama-3.1-8B를 4bit로 올리면 가중치만 약 5.5GB로 빠듯하고, QLoRA 학습까지는
메모리가 부족하다. 부족분이 시스템 메모리로 넘어가면 에러 없이 **10배 이상 느려진다.**
실험 반복 횟수를 확보하는 것이 우선이라 판단해 3B를 주력으로 선택했다.
(8B는 여유가 되면 추론 전용으로 비교군에 추가)

---

## 사전 작업 — 공용 코드 수정 (PR #15, 머지 완료)

라마 실행 준비 중 발견한 문제 3가지를 팀 공용 코드에 반영했다.
**모두 기존 동작을 바꾸지 않도록** 만들었다.

| 파일 | 변경 | 이유 |
|---|---|---|
| `src/main.py` | `--temperature` 옵션 추가 (기본 0.2 유지) | 재현성 확보. `main.py`가 값을 전달하지 않아 CLI로 조절 불가했음 |
| `src/rag_chain.py` | 연산 dtype 자동 선택 | `float16` 고정이었음. Llama-3는 bf16 학습 모델이라 fp16에서 불안정할 수 있으나, bf16 미지원 GPU(T4 등)를 고려해 하드코딩 대신 자동 감지 |
| `requirements.txt` | `bitsandbytes` 설치 안내 주석 | `--load_in_4bit` 옵션은 있는데 필요한 패키지가 목록에 없어 ImportError 발생 |

> 이전 PR #13에서는 `.gitignore`에 학습 산출물(`models/`, `*.safetensors` 등) 제외 규칙을 추가했다.
> 파인튜닝 결과물이 수 GB라 실수로 커밋되면 팀 전체가 영향을 받기 때문.

---

## 실험 1 — 프롬프트 4종 비교 (기준선)

**가설** 프롬프트 형식을 바꾸면 답변 품질이 개선될 것이다.

**설정** 프롬프트만 변경, 나머지 전부 고정

```
model=Llama-3.2-3B-Instruct(4bit) / top_k=3 / search=similarity
chunk=500 / embed=ko-sbert / temperature=0 (greedy)
```

`--temperature 0`으로 고정해 실행할 때마다 같은 결과가 나오도록 했다.
(문항이 14개뿐이라 샘플링 무작위성만으로도 점수가 흔들리기 때문)

### 결과 — 답변 품질

| 프롬프트 | BERTScore F1 | keyword_rate | citation_acc | 답변 길이 | latency |
|---|---|---|---|---|---|
| basic | 0.6924 | 0.3027 | 0.2917 | 306자 | 5.84초 |
| cite | 0.7100 | 0.2588 | 0.2917 | 228자 | 4.36초 |
| cot | 0.6977 | **0.3426** | 0.2500 | 240자 | 4.53초 |
| **strict** | **0.7159** | 0.2881 | **0.3750** | 222자 | 4.17초 |

### 결과 — 검색 지표 (4종 **완전히 동일**)

| 지표 | 값 |
|---|---|
| Hit@k | 0.5 |
| Recall@k | 0.375 |
| Precision@k | 0.2222 |
| MRR | 0.4167 |
| **refusal_acc** | **0.0** |

### 유형별 BERTScore F1

| 유형 | basic | cite | cot | strict |
|---|---|---|---|---|
| single (9) | 0.7259 | 0.7479 | 0.7135 | 0.7410 |
| multi (3) | 0.6660 | 0.6811 | 0.7061 | 0.7225 |
| unanswerable (2) | 0.5813 | 0.5833 | 0.6140 | 0.5931 |

---

## 발견

### 발견 1. 프롬프트를 바꿔도 검색 지표는 소수점까지 동일하다

버그가 아니라 **당연한 결과**다. 프롬프트는 검색이 끝난 *다음* 단계에서 작동하므로
검색에 개입할 수 없다. 동시에 이것은 **변수 통제가 제대로 됐다는 증거**이기도 하다.

→ **검색 실패는 프롬프트 엔지니어링으로 고칠 수 없다.**

주의: "프롬프트가 무의미하다"는 뜻은 아니다. `strict`는 인용 정확도에서
0.375로 다른 프롬프트(0.25~0.29)보다 눈에 띄게 높았다.
프롬프트는 *답변의 형식과 태도*에는 영향을 준다.

### 발견 2. 정답 조문의 절반을 못 찾는다 (Hit@k = 0.5)

`references.csv`에 미리 적힌 정답 조문과 기계적으로 대조한 결과,
검색 대상 12문항 중 **6문항에서 정답 조문을 하나도 가져오지 못했다.**

| 문항 | 난이도 | 정답 근거 | 실제 검색 결과 | 판정 |
|---|---|---|---|---|
| L-H | 하 | 법 제38조 | 법 제6조, 제63조, 제80조 | ❌ |
| L-M | 중 | 법 제52조 | (정답 포함) | ✅ |
| L-S | 상 | 법 제167조 | 시행령 제110조, 법 제2조, 제55조 | ❌ |
| D-H | 하 | 시행령 제14조 + 별표2 | (일부 포함) | ✅ |
| D-M | 중 | 시행령 제52조 | 법 제19조, 제15조, 시행령 별표3 | ❌ |
| D-S | 상 | 시행령 제16조 + 별표3 | 시행령 제18조, 법 제17조, 시행령 제52조 | ❌ |
| R-H | 하 | 규칙 제37조 | (정답 포함) | ✅ |
| R-M | 중 | 규칙 제618조 | 규칙 제619조의2 ×2, 별표18 | ❌ |
| R-S | 상 | 규칙 제140조·제143조 | (정답 포함) | ✅ |
| M1 | 상 | 규칙 제37조 + 법 제38·167조 | (일부 포함) | ✅ |
| M2 | 상 | 규칙 제618조 + 법 제38·39·167조 | 규칙 제619조의2, 시행령 제110조, 규칙 제635조 | ❌ |
| M3 | 중 | 규칙 제37조·제140조·제143조 | (일부 포함) | ✅ |

**난이도 '하'인 L-H조차 실패**했다는 점이 중요하다.

### 발견 3. 실패 원인 — 어휘에 끌려가고, 개념으로 못 찾는다

실패 사례를 뜯어보면 공통 패턴이 보인다.

**(a) 질문 속 단어에 끌려감 — L-H**
질문 *"근로자의 안전을 위한 조치를 취할 의무는 누구에게 있나요?"*
→ 정답은 제38조(**사업주**의 안전조치)인데, 질문에 있는 "근로자"라는 단어 때문에
제6조(**근로자**의 의무)가 1순위로 검색됨.

**(b) 어휘가 구조적으로 안 겹침 — L-S** ⭐
질문 *"안전조치 의무 위반으로 근로자가 사망하면 어떤 처벌을 받나요?"*
→ 정답은 벌칙 조문인 제167조. 그런데 벌칙 조문의 본문은
*"제38조를 위반하여 근로자를 사망에 이르게 한 자는 7년 이하의 징역…"* 형태라
**"처벌"이라는 단어가 아예 등장하지 않는다.**
의미 기반 검색만으로는 구조적으로 찾기 어려운 케이스.

**(c) 인접 조문에 끌려감 — D-S, D-M**
제16조 대신 제18조, 제52조 대신 제19조 등 비슷한 주제의 옆 조문을 가져옴.

### 발견 4. 중복 청크가 검색 자리를 잡아먹는다 (R-M)

```
top_k = 3 인데 실제로는:
  1. 규칙 제619조의2   ← 같은 조문
  2. 규칙 제619조의2   ← 같은 조문 (중복!)
  3. 규칙 별표0018
```

500자로 자르면서 한 조문이 여러 조각이 되었고, 그 조각들이 나란히 상위에 올라왔다.
**top_k=3을 줬는데 실질적으로 2개 문서만 본 셈**이다.
`precision@k = 0.2222`가 낮은 원인 중 하나.

### 발견 5. 🚨 모르는 질문을 거절하지 못한다 (refusal_acc = 0.0)

`strict` 프롬프트에는 *"근거에서 답을 찾을 수 없으면 '산업안전보건법령에서 해당 내용을
찾을 수 없습니다'라고만 답하세요"* 가 명시되어 있다. **그럼에도 4개 프롬프트 전부 실패했다.**

> **Q (U1). 지난달에 개정된 산업안전보건법 최신 내용을 알려주세요.**
> A. 산업안전보건법 시행령 별표0014의00에 따르면, 안전보건진단의 종류 및 내용은
> 다음과 같이 규정되어 있습니다. 1. 안전보건조사 2. 안전보건진단 3. 안전보건감사 …

→ 질문과 무관한 별표 내용을 그대로 지어냄.

> **Q (U2). 오늘 우리 현장 상황상 크레인을 돌려도 법적으로 문제없을지 판단해 주세요.**
> A. 크레인이 돌려도 법적으로 문제가 없다는 것은 크레인이 돌려도 법적으로 문제가
> 없다는 것을 의미합니다.

→ 동어반복. 3B 모델의 한계가 드러남.

**산업안전 도메인에서 "모른다"를 말하지 못하는 것은 치명적이다.**

주의: `refusal_acc`는 2문항 기준이라 정량적 근거로는 약하다.
발표에서는 숫자보다 위 답변 원문을 그대로 제시하는 편이 설득력이 높다.

### 부수 관찰

- **한자 혼입**: `--temperature 0.2`(샘플링)로 단발 테스트했을 때는 `瞬間풍속`처럼
  한자가 섞여 나왔으나, `--temperature 0`(greedy)에서는 basic/cite 0건, cot 1건,
  strict 2건으로 줄었다. 재현성 확보의 부수 효과로 보인다.
- **최고 관련도 점수가 낮다**: 정답을 제대로 찾은 경우에도 relevance score가
  0.2975 수준. 추후 `--score_threshold` 설정 시 이 분포를 먼저 확인해야 한다.

---

## 실험 1 결론

> 프롬프트는 검색 단계에 개입할 수 없다는 것이 실험으로 확인되었고(검색 지표 완전 동일),
> 답변 품질 개선 효과도 미미했다(F1 0.69~0.72, 14문항 기준 노이즈 수준).
> 정답 조문과 대조한 결과 **검색이 절반을 놓치고 있어** 이것이 최우선 개선 대상이다.
> 별도로 **답이 없는 질문을 거절하지 못하는 문제**는 검색 개선과 무관하므로
> 시스템 차원의 게이트가 필요하다.

개선 트랙이 둘로 나뉜다.

```
① 검색 실패  →  청크 / 임베딩 / top_k / 검색방식 개선     (12문항 대상)
② 거절 실패  →  score_threshold 시스템 게이트 + 파인튜닝   (2문항 대상)
```

---

## 실험 2 — 검색 조건 스윕 (청크 × 검색방식 × top_k)

**가설** 임베딩의 128토큰 제한 때문에 500자 청크의 뒷부분이 잘리고 있다.
청크를 줄이면 잘림이 사라져 Hit@k가 오를 것이다.

**설정** LLM을 아예 띄우지 않고 검색만 측정 (`src/run_retrieval_sweep.py`).
실험 1에서 프롬프트 4종의 검색 지표가 소수점까지 같았으므로,
검색 지표는 LLM과 무관하다는 것이 이미 확인되었다.

| 변수 | 값 |
|---|---|
| 청크 크기 | 500 (1,953개) / 300 (2,763개) / 200 (4,096개) |
| 검색방식 | similarity / mmr |
| top_k | 3 / 5 / 10 |

총 18조합.

### 결과를 읽는 법 — 반드시 먼저 볼 것

검색 대상이 **12문항**(unanswerable 2문항 제외)이므로
Hit@k는 **0.0833 단위로만** 움직인다.

| Hit@k | 실제 의미 |
|---|---|
| 0.5 | 6/12 문항 |
| 0.5833 | 7/12 |
| 0.6667 | 8/12 |
| 0.75 | 9/12 |
| 0.8333 | 10/12 |

**표에 나타난 차이의 대부분이 "문항 1~2개"다.** 이 점을 빼고 해석하면 안 된다.

### 결과 — 청크 크기별 Hit@k (검색방식 similarity)

| top_k | 청크 500 | 청크 300 | 청크 200 |
|---|---|---|---|
| 3 | 0.5 | 0.5 | 0.5833 |
| 5 | 0.6667 | 0.6667 | 0.6667 |
| 10 | **0.75** | **0.75** | **0.75** |

### 결과 — 상위 조합

| 임베딩 조건 | 검색방식 | top_k | Hit@k | Recall | Precision | MRR | 중복 |
|---|---|---|---|---|---|---|---|
| 청크 200 | mmr | 10 | **0.8333** | 0.5972 | 0.1333 | 0.4548 | 0.4286 |
| 청크 300 | similarity | 10 | 0.75 | 0.5833 | 0.15 | 0.5092 | 1.00 |
| 청크 500 | similarity | 10 | 0.75 | 0.6112 | 0.1333 | 0.4661 | 0.7857 |
| 청크 300 | mmr | 3 | 0.6667 | 0.4305 | **0.2498** | **0.5278** | 0.0714 |
| *(기준선)* 청크 500 | similarity | 3 | 0.5 | 0.375 | 0.2222 | 0.4167 | 0.0714 |

### 발견 6. 가설이 반증되었다 — 청크 크기는 거의 효과가 없다 ❌

`top_k`를 5 또는 10으로 고정하면 **청크를 어떻게 바꿔도 Hit@k가 완전히 동일하다.**
`top_k=3`에서만 500→200으로 문항 1개가 늘었을 뿐이다.

원인 추정: 청크를 줄이면 잘림은 줄지만 **조문이 더 잘게 쪼개진다**(1,953 → 4,096개).
실제로 청크 200 + similarity + top_k 10에서 중복이 **1.21개**로 표 전체 최고였다.
얻은 만큼 잃은 셈이다.

> ⚠️ 단, 이 실험은 128토큰 제한을 **피해가려는** 시도였지 제한 자체를 없앤 것이 아니다.
> 가설의 진짜 검증은 긴 입력을 받는 임베딩 모델로 교체하는 것이며, 실험 2-2에서 다룬다.

### 발견 7. 실제 지렛대는 top_k였다 — 하지만 공짜가 아니다

| top_k | 최고 Hit@k | Precision |
|---|---|---|
| 3 | 0.6667 (8문항) | 0.25 |
| 5 | 0.6667 (8문항) | 0.20 |
| 10 | 0.8333 (10문항) | 0.13 |

`top_k`를 10으로 올리면 Hit@k는 오르지만 **Precision이 절반으로 떨어진다.**
LLM에게 관련 없는 근거를 두 배로 던져주는 것이므로,
검색 지표만 보고 top_k를 키우면 답변 품질이 오히려 나빠질 수 있다.
**검색 지표 개선이 곧 최종 성능 개선은 아니다.**

### 발견 8. MMR은 중복을 확실히 줄인다 ✅

이것은 통계적 노이즈가 아니라 **기계적으로 설명되는 효과**라 신뢰할 수 있다.

| 설정 (top_k=10) | similarity 중복 | mmr 중복 |
|---|---|---|
| 청크 500 | 0.79 | **0.21** |
| 청크 300 | 1.00 | **0.14** |
| 청크 200 | 1.21 | **0.43** |

실험 1의 R-M 문항에서 발견한 "같은 조문이 top_k 자리를 두 번 차지하는" 문제를
MMR이 실제로 해결한다.

반면 MMR의 **Hit@k 개선 효과는 조합마다 오르내려** 노이즈로 봐야 한다.

### 실험 2 결론

기준선과 같은 `top_k=3` 조건에서 가장 나은 조합은 **청크 300 + MMR**이었다.

| | 기준선 | 개선안 |
|---|---|---|
| 설정 | 500 / similarity / 3 | **300 / mmr / 3** |
| Hit@k | 0.5 (6문항) | 0.6667 (8문항) |
| MRR | 0.4167 | **0.5278** |
| Precision | 0.2222 | **0.2498** |

세 지표가 동시에 개선된 유일한 조합이지만, **문항 2개 차이라 확언할 수 없다.**

> **이번 실험의 가장 큰 교훈은 결과 자체가 아니라 측정의 한계였다.**
> 18개 조합을 비교했지만 대부분의 차이가 문항 1~2개 수준이었다.
> 평가셋 14문항으로는 이 정도 차이를 통계적으로 구분할 수 없다.

---

## 실험 2-2 — 임베딩 모델 교체 ⭐ 프로젝트 최대 성과

**가설** 실패의 원인은 청크 길이가 아니라 **임베딩 모델 자체**다.

기존 `jhgan/ko-sbert-nli`의 문제로 두 가지를 의심했다.

1. 최대 입력이 128토큰 → 긴 청크의 뒷부분을 잘라먹는다
2. 애초에 **검색용이 아니라 문장 유사도(NLI)용**으로 학습된 모델이다

**설계 — 2×2 대조** (`src/run_embedding_sweep.py`, 총 24조합)

ko-sbert가 청크 1000에서 떨어지고 bge-m3는 유지된다면 ①(잘림)이 원인,
청크와 무관하게 bge-m3만 오른다면 ②(모델 품질)가 원인이다.
실험 2에는 이 대조군이 없어서 결론을 내릴 수 없었다.

### 결과 — 2×2 대조표 (similarity 고정)

| top_k=3 | 청크 500 | 청크 1000 |
|---|---|---|
| ko-sbert (128토큰) | 0.5 | 0.5833 |
| **bge-m3 (8192토큰)** | **0.8333** | **0.8333** |

| top_k=5 | 청크 500 | 청크 1000 |
|---|---|---|
| ko-sbert | 0.6667 | 0.6667 |
| **bge-m3** | **0.8333** | **0.9167** |

### 결과 — 같은 조건에서 임베딩만 교체 (청크 500 / similarity / top_k 3)

| | ko-sbert | bge-m3 | 변화 |
|---|---|---|---|
| Hit@k | 0.5 (6/12문항) | **0.8333 (10/12문항)** | **+4문항** |
| Recall@k | 0.375 | **0.7083** | +89% |
| Precision@k | 0.2222 | **0.3887** | +75% |
| MRR | 0.4167 | **0.7083** | +70% |

### 발견 9. 임베딩 모델이 진짜 원인이었다 ✅

**문항 4개 차이**다. 실험 2에서 본 "문항 1~2개 = 노이즈" 수준을 훌쩍 넘고,
**네 지표가 동시에 큰 폭으로** 올랐다. 명확한 신호로 볼 수 있다.

특히 MRR이 0.42 → 0.71로 뛴 것이 중요하다.
정답 조문을 **찾기만 하는 게 아니라 상위에 올려놓는다**는 뜻이다.

### 발견 10. 그러나 "128토큰 잘림"은 주범이 아니었다 ❌

대조군이 가설을 반증했다. **ko-sbert는 청크 1000에서 나빠지기는커녕 오히려 좋아졌다.**
(top_k=3에서 0.5 → 0.5833, top_k=10에서 0.75 → 0.8333)

잘림이 주범이었다면 청크를 키울수록 ko-sbert가 더 손해를 봐야 했다.

**왜 잘림이 예상보다 안 아팠나** — 데이터를 보면 답이 나온다.
조문 본문은 이렇게 시작한다.

```
[산업안전보건기준에 관한 규칙 제37조(악천후 및 강풍 시 작업 중지)]
제37조(악천후 및 강풍 시 작업 중지)
① 사업주는 비ㆍ눈ㆍ바람 또는...
```

**조문을 식별하는 핵심 정보(법령명·조문번호·조문제목)가 맨 앞에 있다.**
128토큰 안에 이미 다 들어가 있으므로, 뒷부분이 잘려도 검색 타격이 작았던 것이다.

즉 문제는 **"얼마나 읽느냐"가 아니라 "무엇을 위해 학습된 모델이냐"**였다.
ko-sbert-nli는 문장 간 함의 관계(NLI)를 학습한 모델이고,
bge-m3는 처음부터 **검색(retrieval)용으로 학습된 모델**이다.

실험 1의 실패 사례 L-S가 이를 뒷받침한다.
벌칙 조문 제167조의 본문에는 "처벌"이라는 단어가 등장하지 않는다
(*"제38조를 위반하여 근로자를 사망에 이르게 한 자는 7년 이하의 징역…"*).
표면 어휘가 겹치지 않는 질문-문서 쌍을 이어주는 것이 검색 전용 모델의 핵심 역량이다.

### 발견 11. 부수 효과 — 청크를 키우면 중복이 준다

조문이 덜 쪼개지므로 같은 조문이 top_k를 여러 칸 차지하는 일이 줄어든다.
bge-m3 + 청크 1000에서는 중복이 낮아 **MMR이 더 이상 필요 없어졌다.**
(bge-m3에서 MMR은 오히려 Hit@k를 약간 깎는다: top_k=3 기준 0.8333 → 0.75)

### 실험 2-2 결론 — 새 기준 설정

| | 기존 기준선 | **새 기준** |
|---|---|---|
| 임베딩 | ko-sbert | **BAAI/bge-m3** |
| 청크 | 500 | **1000** |
| 검색방식 | similarity | similarity |
| top_k | 3 | **5** |
| Hit@k | 0.5 (6/12) | **0.9167 (11/12)** |
| MRR | 0.4167 | **0.7292** |
| Precision@k | 0.2222 | **0.25** |

보통 top_k를 키우면 Precision이 떨어지는데 **여기서는 Precision까지 올랐다.**
검색이 정확해져서 5개를 가져와도 노이즈가 덜 섞이기 때문이다.
**트레이드오프 없이 기준선을 모든 지표에서 이긴 드문 경우.**

> 이 개선은 검색 단계에서 일어나므로 **LLM과 무관하다.**
> 즉 Llama뿐 아니라 Qwen·Mistral·EXAONE에도 그대로 적용된다.
> → 팀 공용 코드에 반영 (PR, 머지 완료)

### 함께 고친 버그 (PR, 머지 완료)

`main.py`의 벡터DB 저장 폴더 이름이 `..._faiss_hf` 형태로 만들어지는데,
`hf`는 "HuggingFace 계열"이라는 뜻일 뿐 **모델명이 들어가지 않았다.**

그래서 임베딩 모델을 바꿔도 예전 벡터DB를 그대로 재사용해버린다.
모델마다 벡터 차원이 다르므로(ko-sbert 768 / bge-m3 1024)
대부분 에러가 나고, 차원이 우연히 같으면 조용히 엉뚱한 결과가 나온다.

수정 내용 (`main.py`만, 총 20줄):

- `--embedding_model` 옵션 추가 (`build_vectorstore.py`에는 인자가 원래 있었으나 전달되지 않고 있었음)
- 벡터DB 폴더명과 결과 파일명에 **모델을 지정했을 때만** 꼬리표 추가
  → 옵션을 안 쓰면 기존 이름 그대로라 팀원들의 벡터DB가 그대로 재사용됨
- `exp_config`에 `embed_model` 기록

```bash
python main.py --embedding_model BAAI/bge-m3 --chunk_size 1000 --top_k 5
```

---

## 실험 4 — bge-m3 검색으로 라마 재평가

**질문** 검색 지표가 올랐다고 해서 답변 품질도 오르는가?

**설정** `src/run_llama_bge.py` — bge-m3 / 청크 1000 / greedy, 4조합

> 검증: 결과의 `hit_at_k`가 실험 2-2와 정확히 일치했다
> (top_k 3 → 0.8333, top_k 5 → 0.9167). 벡터DB를 올바로 잡았다는 확인.

### 결과 — 임베딩만 바꾼 순수 효과 (strict / top_k 3 동일 조건)

| 지표 | 실험 1 (ko-sbert) | 실험 4 (bge-m3) | 변화 |
|---|---|---|---|
| **keyword_rate** | 0.2881 | **0.4880** | **+69%** |
| bertscore_f1 | 0.7159 | 0.7528 | +5% |
| citation_acc | 0.375 | 0.4167 | +11% |
| precision@k | 0.2222 | 0.3887 | +75% |
| MRR | 0.4167 | 0.7083 | +70% |
| latency | 4.17초 | **2.62초** | **-37%** |
| refusal_acc | 0.0 | **0.0** | 변화 없음 |

### 결과 — 4조합 비교 (전부 bge-m3 / 청크 1000)

| | **strict k3** | cite k3 | strict k5 | cite k5 |
|---|---|---|---|---|
| bertscore_f1 | **0.7528** | 0.7395 | 0.7392 | 0.7214 |
| keyword_rate | 0.4880 | **0.5128** | 0.4340 | 0.4536 |
| citation_acc | 0.4167 | 0.4167 | 0.3542 | **0.4931** |
| latency | **2.62초** | 5.35초 | 3.75초 | 5.29초 |
| hit_at_k | 0.8333 | 0.8333 | 0.9167 | 0.9167 |

### 발견 12. 검색 개선이 답변 품질로 이어졌다 — 단, `keyword_rate`가 잡아냈다

가장 크게 움직인 지표는 **`keyword_rate` (+69%)**다.
이것은 정답의 핵심 수치·조문 번호가 답변에 **실제로 들어갔는지**를 재는 지표다.

반면 BERTScore F1은 0.7159 → 0.7528로 +5%에 그쳤다.
**BERTScore가 둔감한 지표이기 때문**이다 — 실험 1에서 크레인 헛소리를 했을 때도
0.71이 나왔다. 문장이 그럴듯하기만 하면 점수를 준다.

> **BERTScore만 봤다면 개선이 미미해 보였을 것이다.
> 키워드 포함률이 진짜 변화를 잡아냈다.**

응답 시간이 37% 줄어든 것도 같은 맥락이다.
근거가 정확해지니 모델이 헤매지 않고 짧고 확실하게 답한다.

### 발견 13. ⭐ 검색 지표 최적 ≠ 최종 성능 최적

**검색은 top_k 5가 낫지만(Hit@k 0.9167 vs 0.8333), 최종 답변은 top_k 3이 모든 면에서 낫다.**

| | top_k 3 | top_k 5 |
|---|---|---|
| Hit@k (검색) | 0.8333 | **0.9167** |
| bertscore_f1 | **0.7528** | 0.7392 |
| keyword_rate | **0.4880** | 0.4340 |
| citation_acc | **0.4167** | 0.3542 |

근거를 5개 주면 노이즈 2개가 더 섞이는데, **3B 모델이 그것을 걸러낼 능력이 부족하다.**
3개만 주면 집중해서 답한다.

**Hit@k만 보고 튜닝했다면 잘못된 설정을 골랐을 것이다.**
실험 2에서 "top_k를 무작정 키우면 답변 품질이 나빠질 수 있다"고 적어둔 우려가
데이터로 확인된 셈이다.

### 발견 14. 프롬프트 효과는 목적에 따라 갈린다

`cite`의 인용 정확도 우위는 **top_k=5에서만** 나타났다 (0.4931 vs strict 0.3542).
top_k=3에서는 둘 다 0.4167로 동일했다.
근거가 많을 때 번호 표기 지시가 더 유용해지는 것으로 보이나,
12문항 기준이라 단정하기 어렵다.

### 실험 4 결론 — 최종 설정 확정

```
임베딩    BAAI/bge-m3
청크      1000자
검색      similarity
top_k     3
프롬프트   strict
```

**압도적 승자는 없었다.** `strict/k3`는 F1 최고이자 속도가 2배 빠르고 나머지도 준수해
종합 최적으로 판단했다. 인용 정확도만 놓고 보면 `cite/k5`(0.4931)가 높다.

### 남은 문제

| 항목 | 결과 |
|---|---|
| **refusal_acc** | **0.0 — 변화 없음** |
| multi 유형 F1 | 0.7225 → 0.6623 (**하락**) |

검색을 아무리 고쳐도 "모른다"는 못 한다. U1·U2는 애초에 법령에 답이 없는 질문이므로
검색 개선과 무관하다. → **실험 3의 명분**

복합 질문(multi)이 오히려 떨어진 것은 여러 조문을 엮는 3B 모델의 한계로 보인다.

---

## 실험 3 — 거절 게이트 (score_threshold)

**문제** 실험 1~4 내내 `refusal_acc = 0.0`.
*"근거를 찾을 수 없으면 '찾을 수 없습니다'라고만 답하라"*고 명시한 `strict` 프롬프트조차
실패했고, 검색을 크게 개선한 뒤에도 그대로였다.

**접근** 프롬프트(모델에게 부탁)가 아니라 **시스템(코드로 차단)**으로 해결한다.
`rag_chain.py`에 이미 구현된 `score_threshold`를 사용해,
검색 최고 관련도가 임계값 미만이면 **LLM을 아예 호출하지 않고** 거절한다.

### 임계값 근거 — 실측 분포 (bge-m3 / 청크1000 / similarity / top_k 3)

| 그룹 | 관련도 범위 |
|---|---|
| 정상 문항 12개 | **0.4573 ~ 0.6985** |
| 답변불가 U1, U2 | **0.4171, 0.4233** |

**두 그룹이 겹치지 않는다.** 경계에 해당하는 0.45를 임계값으로 선택했다.

| 임계값 | U1·U2 거절 | 정상 문항 오거절 | 판정 |
|---|---|---|---|
| 0.40 | 0/2 | 0/12 | 못 거름 |
| **0.45** | **2/2** | **0/12** | **최적** |
| 0.50 | 2/2 | 3/12 | 과잉 차단 |
| 0.60 | 2/2 | 5/12 | 과잉 차단 |

> ⚠️ **한계를 반드시 함께 밝힐 것.**
> 답변불가 문항이 2개뿐이라 **표본 2개에 맞춘 임계값**이다.
> 여유 폭도 0.034(0.4233 → 0.4573)로 좁다.
> 실제 서비스라면 훨씬 많은 데이터로 재조정해야 한다.

### 실행

`src/run_llama_gate.py` — 임계값 0.45와 0.50 두 개.
0.50은 **과잉 차단이 실제로 일어나는지 보여주는 대조군**이며,
"왜 0.45를 골랐는가"에 대한 답이 된다.

### 결과

| 지표 | 게이트 없음 | **임계값 0.45** | 임계값 0.50 |
|---|---|---|---|
| **refusal_acc** | **0.0** | **1.0** ✅ | 1.0 |
| bertscore_f1 | 0.7528 | **0.7574** ↑ | 0.7492 ↓ |
| keyword_rate | 0.4880 | **0.4880** (동일) | 0.4134 ↓↓ |
| citation_acc | 0.4167 | **0.4167** (동일) | 0.2500 ↓↓ |
| hit_at_k | 0.8333 | 0.8333 | 0.8333 |
| latency | 2.62초 | **2.34초** | 1.82초 |
| unanswerable F1 | 0.6855 | **0.7173** ↑ | 0.7173 |
| **LLM 호출 횟수** | 14 / 14 | **12 / 14** | 9 / 14 |

### 발견 15. ⭐ 프롬프트로 0%였던 것이 시스템으로 100%가 됐다

`refusal_acc 0.0 → 1.0`. 그것도 **다른 지표를 하나도 희생하지 않고** 달성했다.

- `keyword_rate`, `citation_acc`, `hit_at_k` — **소수점까지 완전히 동일**
  → 정상 문항은 단 하나도 잘못 차단되지 않았다는 직접 증거
- `bertscore_f1`은 오히려 상승 (0.7528 → 0.7574)
  → 답변불가 문항이 헛소리 대신 정확한 거절 문구를 내놓아 정답과 가까워졌다
- `latency`도 감소 (2.62 → 2.34초)
  → 2문항은 LLM을 아예 호출하지 않으므로

**LLM 호출 횟수가 예측과 정확히 일치했다.**

| | 예측 | 실제 |
|---|---|---|
| 임계값 0.45 | 14 − 2 = 12회 | **12회** |
| 임계값 0.50 | 14 − 5 = 9회 | **9회** |

관련도 분포로 계산한 시뮬레이션이 실제 동작과 그대로 맞아떨어졌다.

### 발견 16. 과잉 차단은 검색 지표가 아니라 생성 지표에 나타난다

임계값 0.50에서 정상 문항 3개가 잘못 차단됐는데, **`hit_at_k`는 0.8333 그대로였다.**

`rag_chain.py`는 거절할 때도 검색된 출처를 기록해두기 때문이다(디버깅용).
따라서 **검색 지표만 보면 과잉 차단을 놓친다.**

실제로 신호가 나타난 곳은 생성 지표였다.

| 지표 | 0.45 | 0.50 | 변화 |
|---|---|---|---|
| citation_acc | 0.4167 | 0.2500 | **-40%** |
| keyword_rate | 0.4880 | 0.4134 | **-15%** |

> 이 실험을 설계할 때는 "과잉 차단이면 `hit_at_k`가 떨어질 것"이라고 예상했으나
> **그 예상은 틀렸다.** 지표가 무엇을 측정하는지 정확히 알아야
> 올바른 지표를 볼 수 있다는 교훈.

또 하나 주의할 점: 임계값 0.50은 `latency`가 가장 짧고(1.82초) `refusal_acc`도 1.0이다.
**속도와 거절률만 보면 0.50이 더 좋아 보인다.** 실제로는 정상 질문 3개를 막아버린
더 나쁜 설정인데도 그렇다.

### 실험 3 결론

```
score_threshold = 0.45
```

**"프롬프트로 안 되면 시스템으로 막는다"**가 데이터로 증명됐다.
산업안전 도메인에서 챗봇이 "모른다"고 말할 수 있게 되었고,
정상 질문의 응답 품질은 전혀 손상되지 않았다.

> ⚠️ 발표에서 반드시 함께 밝힐 한계:
> 답변불가 문항이 2개뿐이며 두 그룹의 여유 폭이 0.034에 불과하다.
> **임계값 0.45는 표본 2개에 맞춘 값**이므로, 실제 서비스라면
> 훨씬 많은 데이터로 재조정해야 한다.

### 최종 설정 (실험 1~4 종합)

```
모델      meta-llama/Llama-3.2-3B-Instruct (4bit)
임베딩    BAAI/bge-m3
청크      1000자 / overlap 50
검색      similarity, top_k 3
프롬프트   strict
거절게이트  score_threshold 0.45
디코딩     greedy (temperature 0)
```

| 지표 | 최초 기준선 | **최종** | 개선 |
|---|---|---|---|
| Hit@k | 0.5 | 0.8333 | +67% |
| MRR | 0.4167 | 0.7083 | +70% |
| Precision@k | 0.2222 | 0.3887 | +75% |
| keyword_rate | 0.2881 | 0.4880 | +69% |
| bertscore_f1 | 0.7159 | 0.7574 | +6% |
| **refusal_acc** | **0.0** | **1.0** | — |
| latency | 4.17초 | 2.34초 | -44% |

**파인튜닝 없이, 전처리·검색·시스템 설계만으로 달성했다.**

---

## 발표 스토리라인 (안)

> ### "절반을 못 찾고, 모르는 걸 지어냈다"
> #### — 프롬프트가 아니라 검색을 고쳤다

| 막 | 내용 | 증거 |
|---|---|---|
| 1. 문제 제기 | 안전 법령 챗봇인데 근거를 절반만 찾고, 답이 없는 질문에 헛소리를 한다 | Hit@k 0.5 / refusal_acc 0.0 |
| 2. 첫 시도와 실패 | 프롬프트 4종을 바꿔봤다 → **검색 지표 변화 0** | 실험 1, 4종 지표 완전 동일 |
| 3. 두 번째 시도와 반증 | 청크를 줄여 잘림을 피해봤다 → **거의 효과 없음** | 실험 2, top_k 고정 시 청크 무관 |
| 4. 원인 규명 | 문제는 길이가 아니라 **임베딩 모델 자체**였다 (NLI용 ≠ 검색용) | 실험 2-2, 2×2 대조표 |
| 5. 해결 | bge-m3 교체 → **Hit@k 0.5 → 0.9167** | 실험 2-2, 팀 전체에 PR 반영 |
| 6. 그래서 답변도 좋아졌나 | **키워드 포함률 +69%**, 응답시간 -37% | 실험 4 |
| 7. 두 번째 반전 | **검색 지표 최적 ≠ 최종 성능 최적** (top_k 5 < 3) | 실험 4 비교표 |
| 8. 안전장치 | 프롬프트로 0%였던 거절을 **시스템 게이트로 100%** — 비용 0 | 실험 3 |
| 9. 파인튜닝 | 형식·인용·한국어 출력 교정 | 실험 5 |
| 10. 한계와 교훈 | **14문항으로는 문항 1~2개 차이를 구분할 수 없었다** | 실험 2의 스윕 표 |

**핵심 메시지: "프롬프트로 안 되면 시스템으로 막는다"**

> 3막(가설 반증)을 숨기지 말고 그대로 보여주는 것이 좋다.
> "예상이 틀렸고, 그래서 원인을 다시 찾았다"는 흐름이
> "처음부터 맞췄다"보다 훨씬 설득력 있고 실제 연구 과정에 가깝다.
>
> 5막은 **팀 전체 기여**로 강조할 것.
> 검색 개선은 LLM과 무관하므로 4명 모두의 성능이 함께 올랐다.

---

## 다음 실험 계획

### 평가셋 확대 (실험 2에서 드러난 최우선 과제)

지금 실험의 결론이 계속 "잘 모르겠다"로 끝나는 이유는 **12문항으로는
문항 1~2개 차이를 구분할 수 없기 때문**이다.

40문항 정도로 늘리면 팀 전체 실험의 신뢰도가 함께 올라간다.
평가셋은 공용 자산이므로 팀과 협의 후 진행할 것.

> 단, 실험 2-2에서 Hit@k가 크게 뛴다면(예: 문항 5개 이상 차이)
> 문항 수 부족과 무관하게 결정적 증거가 되므로, 그 결과를 먼저 확인한다.

### 실험 4 — bge-m3 검색으로 라마 재평가 (다음 차례)

지금까지 개선된 것은 **검색 지표**뿐이다.
라마가 그 근거를 실제로 잘 활용하는지는 아직 확인되지 않았다.

`src/run_llama_bge.py` 로 3조합을 돌린다.

| 조합 | 임베딩 | 청크 | 프롬프트 | top_k | 목적 |
|---|---|---|---|---|---|
| 4a | bge-m3 | 1000 | strict | 3 | **실험 1과 top_k를 맞춘 대조군** |
| 4b | bge-m3 | 1000 | strict | 5 | 새 최적 설정 |
| 4c | bge-m3 | 1000 | cite | 5 | 프롬프트 확인 |

4a가 중요하다. 실험 1(strict/k3/ko-sbert)과 비교하면
**순수한 임베딩 효과**만 분리해서 볼 수 있다.
그렇지 않으면 "top_k를 키워서 좋아진 것 아니냐"는 지적에 답할 수 없다.

**비교 기준 (실험 1, strict)**

| 지표 | 값 |
|---|---|
| bertscore_f1 | 0.7159 |
| citation_acc | 0.375 |
| refusal_acc | 0.0 |

**해석 방향**

| 결과 | 의미 | 다음 |
|---|---|---|
| F1·citation 상승 | 검색 개선이 최종 성능으로 이어짐 | 파인튜닝으로 마무리 |
| 변화 없음 | 검색은 고쳤는데 3B가 활용하지 못함 | **파인튜닝의 명분이 됨** |

어느 쪽이 나와도 발표 재료가 된다.

> 검증 포인트: 결과의 `hit_at_k`가 실험 2-2와 같은 값
> (top_k 3 → 0.8333, top_k 5 → 0.9167)으로 나와야 정상이다.
> 다르면 벡터DB를 잘못 잡은 것이다.

### 실험 3 — 거절 게이트 (score_threshold)

`retrieval_sweep_detail.csv` / `embedding_sweep_detail.csv`에 문항별 `top_score`를
기록해두었으므로, 이 분포를 그대로 쓰면 된다.

1. 최적 설정에서 전 문항의 relevance score 분포를 확인
2. 정답 문항의 최저 점수와 U1/U2의 점수 사이에서 임계값 후보를 잡음
3. `--score_threshold` 값을 바꿔가며 refusal_acc ↔ Hit@k 트레이드오프 측정

### 실험 5 — QLoRA 파인튜닝

- 학습 데이터: `data/ft_dataset.json` (현재 100건, chat 형식 JSONL)
- **100건은 부족**. `fetch_law.py` 산출물로 300~500건까지 증강 예정
- ⚠️ **`eval/questions.csv`의 14문항과 겹치는 내용은 반드시 제외** (데이터 누수)
- 기대 효과와 검증 포인트:

| 지표 | 예상 | 이유 |
|---|---|---|
| Hit@k | 거의 변화 없음 | 검색은 건드리지 않으므로 |
| citation_acc | 상승 | 인용 형식을 학습 |
| keyword_rate | 상승 | 답변 스타일 정렬 |
| 한자 혼입 | 감소 | 한국어 출력 교정 |

> **"RAG에서 파인튜닝은 지식을 넣는 것이 아니라 형식과 태도를 가르치는 것이다.
> 지식은 검색이 담당한다."** — 이 점을 데이터로 보여주는 것이 실험 5의 목표.

### 실험 6 — 팀 공통 조건 최종 비교

4개 모델(Llama / Qwen / Mistral / EXAONE)을 **동일 설정**에서 비교.
설정이 제각각이면 모델 차이인지 설정 차이인지 구분할 수 없다.

보고서에는 **① 공통 조건 비교표**와 **② 각자 최적화 결과**를 분리해서 싣는다.

---

## 부록 A — 결과 CSV에 빈 열이 있는 이유

정상 동작이다.

| 열 | 비어 있는 이유 |
|---|---|
| `faithfulness`, `answer_relevancy`, `context_precision`, `context_recall` | RAGAS 지표. `--use_ragas`를 켜야 계산되며 별도 LLM 심판(API 키) 필요 |
| `refusal_acc` | `유형 == unanswerable`인 문항에서만 채점 → U1, U2 2개만 값이 있음 |
| `hit_at_k`, `recall_at_k`, `citation_acc` 등 | `references.csv`에 정답 조문이 있는 문항만 채점 → U1, U2는 빈칸 |

즉 12문항은 검색 지표가, 2문항은 거절 지표가 채워지는 상호보완 구조다.
평균 계산 시 빈칸은 자동으로 제외된다.

> 💡 RAGAS의 `faithfulness`는 "검색된 근거에 없는 내용을 지어냈는지"를 재는 지표로,
> 이번에 발견한 환각 문제와 정확히 맞아떨어진다. 여유가 되면 최종 보고서용으로
> 한 번 돌려볼 가치가 있다.

## 부록 B — 실행 방법

| 스크립트 | 하는 일 | LLM 로드 | 실험 |
|---|---|---|---|
| `src/run_llama.py` | 프롬프트 4종 **전체 평가** (기준선) | O | 1 |
| `src/run_retrieval_sweep.py` | 청크 × 검색방식 × top_k **검색만** | X | 2 |
| `src/run_embedding_sweep.py` | 임베딩 모델 × 청크 **검색만** | X | 2-2 |
| `src/run_llama_bge.py` | bge-m3 설정으로 **전체 평가** | O | 4 |
| `src/run_llama_gate.py` | 거절 게이트 임계값 검증 | O | 3 |

> `run_llama.py`는 실험 1 재현용으로 그대로 둔다.
> 설정을 바꿀 때는 파일을 새로 만들어, 각 실험이 어떤 설정으로 돌았는지
> 코드로 남게 한다.

```bash
cd src
python run_llama.py
```

`run_llama.py` 상단의 `EXPERIMENTS` 목록을 고치면 실험 조합이 바뀐다.
`main.py`를 복사하지 않고 실행만 시키는 구조라, 팀에서 `main.py`를 개선하면
그 내용이 그대로 반영된다.

결과는 `eval/results/llama3.2-3b_<프롬프트>_k<top_k>.csv`로 저장된다.
(루트의 `results/`는 `.gitignore` 대상이므로, 공유할 결과는 `eval/results/`에 넣는다)

## 부록 C — 실행 로그에 뜨는 경고들

| 경고 | 대응 |
|---|---|
| `Setting pad_token_id to eos_token_id:None` | Llama 토크나이저에 pad_token이 없어서 발생. 질문을 하나씩 처리하는 현재 구조에서는 경고일 뿐 문제없음 |
| `do_sample is set to False. However, top_p is set to 0.9` | greedy 모드에서 top_p가 무시된다는 안내. 동작에 영향 없음 |
| `You seem to be using the pipelines sequentially on GPU` | 배치 처리를 권하는 안내. 14문항 규모에서는 무시 가능 |
